Ce widget permet d'afficher un selecteur de catégories en back.
Pour l'utiliser, il faut utiliser un model avec une relation parent/enfant lié à un autre modèle et un inspecteur
et adapter la config du champ (cf config.sample) à insérer dans la config du CRUD
(ou tout autre controlleur utilisant la config des champs de NOS).